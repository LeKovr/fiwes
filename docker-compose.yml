version: '2.1'

services:

  app:
    image: ${DC_IMAGE}
    command:
      --html --http_addr :8080
    ports:
      - "${SERVER_PORT}:8080"
    volumes:
      - /etc/timezone:/etc/timezone:ro
      - /etc/localtime:/etc/localtime:ro
      - ./var/data:/data
    build:
      context: .
      args:
        - GO_VERSION
